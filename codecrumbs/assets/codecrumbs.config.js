module.exports = {
  // Relative path to project source entry point file (must be inside projectDir)
  entryPoint: 'src/index.js',
  // Relative path to project source code directory
  projectDir: 'src',
  // Port for the Codecrumbs client (default 2018)
  clientPort: 2018,
  // Project name alias shown in the UI (defaults to projectDir value)
  projectNameAlias: 'my-project',
  // Enable debug logs
  debugModeEnabled: false
  // excludeDir: 'src/doc,src/thirdparty',
  // webpackConfigPath: 'webpack.config.js',
  // tsConfigPath: 'tsconfig.json',
  // ideCmd: 'code'
};
